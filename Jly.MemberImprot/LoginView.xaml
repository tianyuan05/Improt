<Window x:Class="Jly.MemberImprot.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Jly.MemberImprot"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:xckt="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:cc="clr-namespace:CustomControls;assembly=CustomControls"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        mc:Ignorable="d" 
        WindowState="Maximized" 
        WindowChrome.WindowChrome="{StaticResource MaximizedWindowChrome}"
        Title="LoginView" Height="300" Width="300">
    <Grid >
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0 20 20 0">
            <cc:TextButton Content="&#xe779;" ToolTip="推出" Style="{StaticResource IconTextButtonStyle}"/>
        </StackPanel>
        <DockPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <TextBlock Text="&#xe602;" Style="{StaticResource IconTextBlockStyle}"/>
                <xckt:WatermarkTextBox Width="200" Watermark="输入用户名" VerticalContentAlignment="Center" Background="Transparent" Style="{StaticResource DefaultWatermarkTextBoxStyle}"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 20">
                <TextBlock Text="&#xe606;" Style="{StaticResource IconTextBlockStyle}"/>
                <xckt:WatermarkTextBox Width="200" Watermark="输入用命" VerticalContentAlignment="Center" Background="Transparent" Style="{StaticResource DefaultWatermarkTextBoxStyle}"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Center" VerticalAlignment="Center">
                <cc:BrushButton Content="登录" Height="27" Width="100" Style="{StaticResource DefaultBrushButtonStyle}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
